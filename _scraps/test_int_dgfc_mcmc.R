# ==============================================================================
# load resources
# ==============================================================================

source("_packages.R")
source("_helpers/_helpers.R")

# ==============================================================================
# fake data
# ==============================================================================

set.seed(8675309)

n <- 100
Y <- cbind(rnorm(n), rpois(n, lambda = 10))
p <- ncol(Y)
H = 5

# ==============================================================================
# fake data
# ==============================================================================

draws = DGFC.mcmc(Y)
Ypred = DGFC.forecast(H, draws)

# ==============================================================================
# fake data
# ==============================================================================

i = 1
a = c(0.01, 0.05, 0.1, 0.25, 0.5)

par(mfrow = c(1,1))

plot(ecdf(Y[, i]), cex = 0.1, col = "black")
plot_ma_band(draws$ma[[i]][, 1, 1], draws$ma[[i]][, 2, ], a, rgb(0, 0, 1, 0.1))
